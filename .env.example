# Public URL of the deployed site — used for canonical links, sitemap and OG images.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional analytics id (e.g. Plausible domain or GA4 measurement id).
NEXT_PUBLIC_ANALYTICS_ID=

# Provided automatically on Vercel (used as a fallback for NEXT_PUBLIC_SITE_URL):
# VERCEL_PROJECT_PRODUCTION_URL, VERCEL_URL
